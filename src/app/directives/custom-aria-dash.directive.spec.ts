import { CustomAriaDashDirective } from './custom-aria-dash.directive';

describe('CustomAriaDashDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomAriaDashDirective();
    expect(directive).toBeTruthy();
  });
});
